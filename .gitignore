# User-provided alert audio (keep sounds/.gitkeep in git)
sounds/*.mp3
sounds/*.wav
sounds/*.ogg
sounds/*.m4a
sounds/*.aac
sounds/*.webm

# Saved Frigate GenAI reports (keep reports/.gitkeep in git)
reports/*
!reports/.gitkeep

# Cached Chatterbox TTS recordings (keep localrecordings/.gitkeep in git)
localrecordings/*
!localrecordings/.gitkeep